# SOP Writer

Type a task, vendor product, or procedure — get back a production-ready, parameterized Standard Operating Procedure with a live-editable form and Markdown preview.

## Desktop App (recommended)

Download the installer for your OS from [Releases](https://github.com/bburge14/SOP/releases) — `.exe` (Windows), `.dmg` (Mac), or `.AppImage`/`.deb` (Linux) — and double-click it. No terminal, no `git clone`, no editing config files.

- **First launch** asks you to pick an LLM provider and paste an API key into a Settings screen (gear icon, top right) — stored encrypted via your OS keychain (Keychain on Mac, DPAPI on Windows, libsecret on Linux), not a plaintext file.
- **Updates** are fully in-app: the version badge (top right) checks GitHub Releases, and **Download** → **Restart & Install** applies it — the same pattern as any normal desktop app's auto-updater (`electron-updater`).
- Since there's no paid code-signing certificate behind these builds, **Windows SmartScreen** will show "Windows protected your PC" (click **More info → Run anyway**) and **macOS Gatekeeper** will say the app is from an "unidentified developer" (right-click the app → **Open**, first launch only) — normal for indie-distributed apps, not a sign anything's wrong.

Under the hood this is the exact same Next.js app described below (`app/`, `components/`, `lib/`), wrapped by `electron/` — see that section for how the two distributions relate.

## Architecture

```
app/
  page.tsx                 # renders <SopWorkspace/>
  layout.tsx, globals.css
  api/generate/route.ts    # POST { topic, context? } -> { sop } | { error }
  api/analyze-import/route.ts # POST { document } -> { sop } | { error } — opt-in "Scan with AI" on an import
  api/review-improve/route.ts # POST { document } -> { sop } | { error } — opt-in "Review & Improve" (may rewrite/add content)
  api/suggest-ideas/route.ts  # POST { context } -> { ideas } | { error } — proposes SOP topics from attached docs
  api/clarify/route.ts        # POST { topic, context? } -> { questions } | { error } — "Guided" generation's clarifying-questions step
  api/refine/route.ts         # POST { document, instructions, newInstruction } -> { sop } | { error } — multi-turn "tell it what to change"
  api/update/route.ts      # GET status / POST pull+build+restart, see "Self-updating"

electron/
  main.js                    # spawns the Next standalone server, owns the BrowserWindow, wires electron-updater
  preload.js                  # contextBridge — exposes window.electronAPI to the renderer
  settingsStore.js             # provider/API-key config, persisted in Electron's userData dir
  safeStorageBridge.js          # runs safeStorage in a disposable timed-out helper process — see below

scripts/
  install.sh                # (self-hosted only) clone + npm install + build + optional systemd service
  update.sh                  # (self-hosted only) CLI equivalent of the GUI "Update Now" button
  uninstall.sh                # (self-hosted only) stop/remove the systemd service, optionally purge/delete
  supervisor.mjs               # (self-hosted only) fallback entrypoint (`npm run serve`) for non-systemd hosts
  prepare-standalone.mjs        # copies static assets into .next/standalone, used by both distributions

lib/
  update/
    git.ts                  # thin wrappers over the git/npm CLIs used by the updater
    runner.ts                # orchestrates fetch -> pull -> install -> build -> restart
  llm/
    types.ts               # LlmAdapter interface, LlmAdapterError
    schema.ts               # single source of truth: zod schema + JSON Schema (SOP shape and, separately, the ideas-list shape)
    modelOptions.ts           # curated model IDs + recommended default per provider
    prompt.ts               # system/user prompt builders (generation, import analysis, review, suggest ideas, clarifying questions)
    openai.ts                # GPT — function-calling, tool_choice forced
    gemini.ts                # Gemini — responseSchema + responseMimeType:json
    ollama.ts                 # local models — format:"json" + schema-in-prompt
    retry.ts                   # shared fetchWithRetry() — backs off on 429/5xx, fails fast otherwise
    humanizeError.ts            # raw provider error body -> one clean, actionable sentence
    index.ts                 # getLlmAdapter() factory, reads LLM_PROVIDER
  sop/
    parseJson.ts             # tolerant JSON extraction (fences, prose, trailing commas)
    reconcile.ts              # validates + syncs variables[] against {{placeholders}}
    template.ts                # extractPlaceholders(), renderTemplate() — used by both
                                # the server (reconcile) and the client (live preview)
    markdownToDocx.ts            # markdown -> real .docx (headings/lists/tables/images), client-side
    docxToMarkdown.ts             # imported .docx -> markdown (mammoth + turndown/GFM), client-side
    readFileAsText.ts               # shared file->text reading (routes .docx through docxToMarkdown), used by Import and Attach Reference
    contextLimits.ts                # MAX_CONTEXT_FILES / MAX_CONTEXT_TOTAL_CHARS, shared by client (immediate feedback) and server (enforcement)
    redactSecrets.ts                 # local best-effort scrub of API keys/passwords/tokens before anything is sent to the AI
    db.ts                             # shared IndexedDB plumbing (one db, one upgrade handler) for library.ts + categoryProfiles.ts
    library.ts                       # local SOP library — plain IndexedDB, no network code path at all
    categoryProfiles.ts               # local, reusable per-category context + remembered {{key}} defaults — plain IndexedDB, no network code path
    presetCategories.ts               # static starter list of common SOP categories, merged into the category picker's suggestions
    detectVariables.ts             # local heuristic pass: IPs/MACs/emails/etc -> {{key}} on import, no AI call
    remarkSubstituteVariables.ts    # AST-level {{key}} substitution for the live preview — tags each substituted
                                     # span with data-sop-var for hover-to-locate, no raw HTML injection
    remarkGithubAlerts.ts            # renders `> [!WARNING]` etc. as a distinct callout instead of a plain blockquote
    liveMarkdownExtensions.ts        # CodeMirror 6 extensions for the live-preview Rendered editor — decorations + {{key}} chip widget
  providerInfo.ts             # onboarding copy: per-provider blurb + "get a key" link
  hooks/
    useOnClickOutside.ts        # shared close-on-outside-click for every popover/dropdown in the header/toolbar

components/
  SopWorkspace.tsx          # owns all state, wires everything together
  TopicInput.tsx            # the input bar, Import, Attach Reference, and privacy reminder
  VariableForm.tsx          # dynamic form generated from variables[]; hover a field to locate it in the preview
  MarkdownPreview.tsx       # rendered/source tabs, hover-to-locate scroll+highlight, always-mounted hidden print target
  LiveMarkdownEditor.tsx      # CodeMirror-backed editable "Rendered" tab — live-decorated markdown, not read-only
  LibraryPanel.tsx           # browse/search/filter/load/delete saved SOPs — modal, IndexedDB-backed, no network calls
  MarkdownToolbar.tsx        # Bold/Italic/Strikethrough/Heading/Lists/Task list/Table/Code/Link/HR buttons over the Source textarea
  ActionBar.tsx             # regenerate / add field / scan with AI / review & improve / insert image / copy / export (dropdown) — icon-only, tooltips
  AddFieldDialog.tsx        # small popover form for custom variables
  SplitOccurrencePopover.tsx  # click a {{key}} chip in Rendered: reassign or split off just that one occurrence
  CategoryProfilePanel.tsx    # trigger + popover: view/edit/delete a category's saved context + remembered defaults
  GuidedQuestionsDialog.tsx    # "Guided" generation's clarifying-questions modal — answers feed the next Generate call
  RefinePanel.tsx              # multi-turn "tell the AI what to change" chat-style panel
  ModelSelect.tsx            # provider-aware model dropdown (free text for Ollama)
  UpdatePanel.tsx             # header widget: check for updates / update now (self-hosted, git-based)
  DesktopUpdatePanel.tsx       # header widget: check for updates / update now (desktop, electron-updater)
  DesktopSettingsPanel.tsx      # header gear icon: change provider/model/key after setup
  PreferencesPanel.tsx           # header sliders icon: local display preferences (localStorage, not Electron IPC — works self-hosted too)
  DesktopOnboarding.tsx          # full-screen blocking first-run setup gate (desktop only)

types/sop.ts                 # SopDocument, SopVariable, VariableValues, LlmProvider, CategoryProfile, ClarifyingQuestion

build/icon.png                # electron-builder's source icon — generates the .ico/.icns/.png set for every platform
app/icon.png                   # same image, Next.js's file-based favicon convention (auto-wired, no code)
public/logo.png                 # same image again, used directly in the header/onboarding <img>
```

### Why an adapter interface instead of one SDK

`LlmAdapter` is one method: `generate(systemPrompt, userPrompt, jsonSchema) -> Promise<string>`. Each provider adapter builds its own request with **raw `fetch`** (no `openai` / `@google/generative-ai` packages) and returns the raw JSON text it got back, unvalidated. Parsing and schema validation happen once, centrally, in each API route — so swapping providers via `LLM_PROVIDER` never touches route or frontend code, and the dependency tree stays lean (no LLM SDKs at all). `jsonSchema` used to be hardcoded per-adapter to the single-SOP shape (`sopJsonSchema`); it's now threaded through from the caller so a different response shape — e.g. `sopIdeasJsonSchema` for Suggest Ideas — is actually enforced by each provider's own structured-output mechanism (OpenAI function-calling parameters, Gemini `responseSchema`, Ollama's in-prompt schema hint) instead of every route being silently forced into the single-SOP shape regardless of what it actually asked for.

Each adapter still gets the most reliable structured-output mechanism its provider offers:
- **OpenAI** — function-calling with `tool_choice` pinned to a single tool, so the only valid model output is well-formed arguments.
- **Gemini** — `generationConfig.responseSchema` + `responseMimeType: "application/json"`.
- **Ollama** — `format: "json"` plus the JSON Schema spelled out in the prompt itself, since local-model schema adherence varies. This is the adapter that leans hardest on `lib/sop/parseJson.ts`'s cleanup passes (strip code fences, slice a balanced `{...}` object out of surrounding prose, drop trailing commas) and `lib/sop/reconcile.ts`'s auto-repair (any `{{key}}` used in the markdown but missing from `variables[]` gets synthesized; any declared variable never referenced gets dropped).

All three route their request through `lib/llm/retry.ts#fetchWithRetry` — transient provider errors (429, 500/502/503/504, or a network failure) get up to 3 attempts with exponential backoff (honoring `Retry-After` if the provider sends one) before surfacing to the user; a 4xx like a bad key or bad request fails immediately since retrying it can't help. Reproduced live: Gemini returning "currently experiencing high demand" (503) on an otherwise-working setup — that's now retried transparently instead of requiring a manual re-click.

If a request still fails after retries, `lib/llm/humanizeError.ts` turns the status code + raw provider response into one clean, actionable sentence ("Gemini is temporarily unavailable — try again shortly" / "OpenAI rejected the API key. Check it in Settings." / etc.) instead of surfacing the provider's raw JSON error body. The raw body is preserved as the error's `cause`, logged server-side (`console.error` in `app/api/generate/route.ts`), and sent to the client as a separate `detail` field the error banner shows behind a collapsed "Show technical details" toggle — never dumped in the main message by default, but not lost either.

### Live preview without re-calling the API

`lib/sop/template.ts#renderTemplate` does the `{{key}}` substitution entirely client-side against local React state (`values`). Editing a form field or the raw Markdown source re-renders instantly — the API is only called on Generate/Regenerate.

### Two distributions, one app

`next.config.js` sets `output: "standalone"`, so `npm run build` always produces a pruned, self-contained `.next/standalone/server.js` alongside the normal build. Both distributions serve from that:

- **Self-hosted**: `scripts/supervisor.mjs` or systemd runs `next start` directly against the repo checkout — the git-based updater (`lib/update/`) applies here.
- **Desktop**: `electron/main.js` spawns `.next/standalone/server.js` as a child process and points a `BrowserWindow` at it. `UpdatePanel.tsx` (git-based) and `DesktopUpdatePanel.tsx`/`DesktopSettingsPanel.tsx` (Electron IPC-based) are both always rendered — each one detects its own environment (`isGitCheckout`/`window.electronAPI`) and renders nothing when it doesn't apply, so the same React tree serves both without branching logic in `SopWorkspace.tsx`. The window opens maximized on every launch (`mainWindow.maximize()` on `ready-to-show`, before the window is ever shown so there's no visible flash of the smaller size first) — the fixed `width: 1280, height: 860` passed to `BrowserWindow` is only the *restored* size for if you un-maximize later. Reported live as a real usability problem, not just a preference: at that fixed default size the app's two-pane layout didn't actually fit, and the whole window grew a second, outer scrollbar on top of each pane's own internal one.

Two non-obvious things worth knowing if you touch the packaging config (`package.json`'s `build` field):

- **`safeStorage` can hang the entire app.** Its calls are synchronous on Electron's single main-process thread; on a Linux box with a locked or misconfigured keyring, `isEncryptionAvailable()` was observed to simply never return — freezing the whole app, not just the settings dialog. `electron/safeStorageBridge.js` runs every `safeStorage` call in a disposable child process (this same executable, re-invoked with `--safestorage-helper`) with a hard timeout, falling back to unencrypted storage (with a console warning) rather than hanging if the keychain doesn't respond.
- **electron-builder silently drops any folder named exactly `node_modules`** at the root of an `extraResources`/`extraFiles` copy — a hardcoded rule in `builder-util`'s file-copy filter (`if (relative === "node_modules") return false`), meant to avoid double-copying the main app's own dependencies, with no way to override it via patterns. Since `.next/standalone`'s immediate child is literally `node_modules`, pointing `extraResources.from` straight at it silently produced a server bundle with no dependencies. `scripts/prepare-standalone.mjs` works around this by copying the finished standalone build one directory level deeper (`.next/pkg-src/app-server/`) before packaging, so `node_modules` is never the literal relative root the filter checks against.

## Quickstart (dev)

```bash
npm install
cp .env.example .env.local   # fill in the key for whichever provider you pick
npm run dev                  # http://localhost:3000
```

## Building the desktop app locally

```bash
npm run electron:start   # build + run unpacked, for quick iteration
npm run dist:linux       # AppImage + deb -> release/
npm run dist:win         # nsis installer -> release/ (best built on Windows)
npm run dist:mac         # dmg -> release/ (must be built on macOS, unsigned)
```

`npm run dist*` runs `next build` → `prepare-standalone.mjs` → `electron-builder`, `--publish never` by default so it never tries to upload anywhere. Actual releases are built and published by `.github/workflows/release.yml` on every `v*` tag push (all three platforms, via a build matrix — Windows and Mac can't be reliably cross-built from Linux).

## Self-hosted (advanced)

Skip this section unless you specifically want to run this on a server/always-on machine you control and access it via a browser, rather than using the desktop app above.

### Install

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/bburge14/SOP/main/scripts/install.sh)
```

This clones the repo (default `~/sop-writer`, override with `--dir`), runs
`npm install`, walks you through picking an LLM provider + API key into
`.env.local`, builds a production bundle, and — on Linux with systemd —
registers and starts a `systemd --user` service named `sop-writer`. Run
`loginctl enable-linger $USER` afterward so it keeps running after you log
out. Pass `--no-service` to skip that and just build; `--port PORT` to pick
a port other than 3000.

Already have a checkout? Run `npm run app:install` (or `bash
scripts/install.sh`) from inside it instead — it detects the existing
checkout and installs in place.

### Update

The header's commit badge (git-branch icon + short SHA) opens a panel to
check for and apply updates from the browser. **Update Now** runs, server-side:
`git fetch` → `git pull --ff-only` → `npm install` → `npm run build` → restart.
Or from the CLI: `npm run app:update` (or `bash scripts/update.sh`).

Restart path, in order of preference:
1. **systemd** — if running as the `sop-writer.service` unit `install.sh` set up, the update triggers `systemctl --user restart sop-writer`. This also gets you crash-restart and start-on-boot for free.
2. **supervisor** — if started via `npm run serve` (`scripts/supervisor.mjs`) without systemd (e.g. macOS), the app process exits with a sentinel code the supervisor watches for and relaunches `next start`. No crash-restart — it only handles the update-triggered restart.
3. **manual** — plain `npm run dev` / `npm start`: the update still pulls + rebuilds but reports that you need to restart the process yourself.

Guardrails baked in:
- Refuses to update if the working tree has uncommitted local changes (won't silently discard your edits).
- Only fast-forward pulls (`--ff-only`) — a diverged local history fails loudly instead of merging or resetting.
- Set `UPDATE_TOKEN` (see `.env.example`) if this instance is reachable by anyone besides you — the GUI has a small settings gear next to the update panel to store the matching token in that browser's `localStorage`.

### Uninstall

```bash
npm run app:uninstall                  # stop + remove the systemd service only
bash scripts/uninstall.sh --purge      # also remove node_modules/ and .next/
bash scripts/uninstall.sh --remove-all # also delete the whole install directory (confirmation required)
```

### Environment variables

| Variable | Required when | Notes |
|---|---|---|
| `LLM_PROVIDER` | always | `openai` \| `gemini` \| `ollama` — default `gemini` |
| `OPENAI_API_KEY`, `OPENAI_MODEL` | `LLM_PROVIDER=openai` | model defaults to `gpt-5.6-terra` — see `lib/llm/modelOptions.ts` for current options |
| `GEMINI_API_KEY`, `GEMINI_MODEL` | `LLM_PROVIDER=gemini` | model defaults to `gemini-3.6-flash` — see `lib/llm/modelOptions.ts` for current options |
| `OLLAMA_BASE_URL`, `OLLAMA_MODEL` | `LLM_PROVIDER=ollama` | no API key; point at a running `ollama serve`, defaults to `http://localhost:11434` / `llama3.1` |

## Releases

Tagged as `vX.Y.Z` on GitHub (semver, matching the `version` field in
`package.json`). Pushing a tag triggers `.github/workflows/release.yml`,
which builds and attaches the Windows/Mac/Linux installers plus the
`latest*.yml` metadata `electron-updater` reads to detect new versions.
See [Releases](https://github.com/bburge14/SOP/releases) for changelogs
and downloads. `scripts/install.sh` / `scripts/update.sh` (self-hosted
only) always track the `main` branch, not a specific release tag.

## Privacy & data handling

- **Only the topic string (and, if you've set one, the current category's saved profile context) is ever sent to the AI provider by default — with six further explicit, opt-in exceptions.** `app/api/generate/route.ts` accepts `{ topic, context?, categoryProfile?, clarifications? }` — the values you type into the variable form (hostnames, IPs, credentials, anything site-specific) are substituted entirely client-side by `lib/sop/template.ts#renderTemplate` and never leave the browser/app unless you explicitly export/copy. `TopicInput.tsx` shows a standing reminder not to put proprietary, confidential, or personal information in the topic itself, since that string does go to a third party — the same applies to a category profile's free-text context, which you write yourself in `CategoryProfilePanel.tsx` for exactly this purpose (see Notable behavior) and which is sent automatically whenever the category field is filled in and a saved profile exists for it, same as the topic, no separate confirm. Six further exceptions are **Scan with AI**, **Review & Improve**, **Attach Reference**, **Guided** generation's clarifying-question answers, and **Refine**'s instructions — see below — the first three gated behind either a `window.confirm` or requiring a deliberate attach action, no "don't ask again"; Guided's answers and Refine's instructions are both sent the moment you submit them (`GuidedQuestionsDialog.tsx`'s "Generate SOP" button, `RefinePanel.tsx`'s "Apply" button), same no-separate-confirm treatment as the topic itself, since you're typing them specifically to be used that way — Refine shows a standing privacy notice instead of a per-message confirm, since re-confirming every single instruction in a back-and-forth editing session would defeat the point (and the initial clarifying-questions call, `app/api/clarify/route.ts`, sends only the topic/attached reference material — no different from a normal Generate). The last exception is **Suggest Ideas** (`app/api/suggest-ideas/route.ts`) — it only ever operates on files you've already attached via Attach Reference, so it doesn't add a new consent gate of its own; it's covered by the same one. In most of these cases, `lib/sop/redactSecrets.ts` runs locally first and strips anything that looks like an API key, password, token, or private key from the outgoing copy before it's ever transmitted — best-effort (common, recognizable shapes; not an exhaustive scanner), and every one of these actions' own warning text says so explicitly rather than implying it's a guarantee. Guided-question answers and Refine instructions both go through this same redaction pass at submit time (`redactedClarifications`/`redactedInstruction` in `SopWorkspace.tsx`). Category profile context is the one exception left unredacted — it's short, hand-typed environment description you write specifically to be reused, not pasted document content, so the same pass that scrubs attached files/documents doesn't apply to it; don't put secrets in it.
- **Nothing generated is persisted unless you explicitly save it — and saved SOPs and category profiles are invisible to the AI until you bring them back into a session.** Through v0.2.18 nothing was saved anywhere at all; the local Library and, since v0.2.26, category profiles (see Notable behavior) reverse that deliberately, but neither has a code path to the network at all: `lib/sop/library.ts` and `lib/sop/categoryProfiles.ts` (sharing one IndexedDB database via `lib/sop/db.ts`) are plain open/put/getAll/delete wrappers that never import `fetch` or touch an API route, so opening the Library panel, browsing, filtering, deleting, and viewing/editing/deleting a category profile are all pure local-disk operations. Loading a saved SOP does exactly what Import already did — populate the same workspace state — which by itself never calls the AI either; the AI only ever runs on your explicit click of Generate/Regenerate/Scan with AI/Review & Improve, same as always, whether the current document came from a fresh generation or a loaded library entry, and a category profile's content only ever reaches the AI when you've typed a matching category and click Generate, same explicit-action pattern. Verified directly rather than just by reading the code: a Playwright session tracked every `xhr`/`fetch` request across an entire save → browse → filter → search → load → delete cycle and confirmed zero requests came from any of it (the only two requests logged the whole run were the self-hosted git-update panel's own unrelated background check, present regardless of the library). Outside the Library and category profiles, everything else is unchanged: no other database, the only other `localStorage` key is the self-hosted updater's unrelated auth token, and the only `fs.write*` calls in `electron/` are the PDF export (to a path *you* pick) and the provider/API-key config file. Server-side error logging (`console.error` in `app/api/generate/route.ts`) logs the exception only, never the topic or the generated document.

## Notable behavior

- **Generation quality rules** (`SOP_SYSTEM_PROMPT` in `lib/llm/prompt.ts`) go beyond "produce a valid schema" to three specific, previously-observed failure modes — there's no reliable static way to catch these in code (no rule can detect "this hardcoded value should track that variable" or "this rollback step is fake"), so the fix lives entirely in the prompt text the model is given:
  - **No hardcoded values coupled to a variable.** If parameterizing a value (e.g. `{{external_port}}`) leaves some other line only correct for the default (e.g. a firewall policy's service hardcoded to `"HTTPS"`, which breaks the moment the port isn't 443), that dependent value must be parameterized too or phrased so it's derived from the variable at execution time.
  - **No redundant variables for one underlying value.** Never split a single physical value across multiple variables the user has to keep in sync by hand — e.g. `{{disk_name}}` + `{{partition_number}}` + `{{pv_device_path}}` for what's really one device path. One canonical variable holding the full value, reused everywhere it's needed.
  - **Rollback/cleanup steps must be real, executable commands** — never bracketed pseudocode like `delete [policy_id_assigned_to_{{policy_name}}]`. If a step needs a runtime-assigned ID, the rule requires the actual lookup command followed by the actual command using its result.
  - **Mandatory pre-flight safety checkpoint** for destructive/hard-to-reverse/broad-impact steps (partition/disk resizing, tenant-wide or broad access/firewall policy changes, deleting/replacing a resource) — prerequisites or step 1 must confirm something like a completed snapshot, a verified restorable backup, or working break-glass access exists *before* anything disruptive happens.

  The same redundancy/coupling rules (not the rollback/safety ones, which are about authoring new content — import analysis is explicitly forbidden from adding steps) are echoed in `IMPORT_ANALYSIS_SYSTEM_PROMPT`, and the `template_markdown`/`prerequisites` field descriptions in `lib/llm/schema.ts` reinforce the same points at the JSON-Schema level for providers that use structured output (OpenAI function-calling, Gemini `responseSchema`), since those field-level descriptions are literally what the model sees when filling that specific field. Verified live end-to-end against a real local model (Ollama, `qwen2.5-coder:7b`) rather than just confirming the prompt text: asked it to extend an LVM logical volume onto a new disk, and unprompted it used one canonical `{{new_disk_partition}}` variable (not three redundant ones), included a step 1 reading "Safety Checkpoint: Verify that a recent verified backup exists and is restorable before proceeding," and gave real `vgreduce`/`lvremove` rollback commands rather than pseudocode.
- **No hedging, a fixed seven-section structure, and the real interaction mode — not the flagged-assumption approach this replaced.** An earlier version (see git history around v0.2.23) had the model mark facts it couldn't verify with a `> [!WARNING]` callout instead of stating them as settled fact. Direct, repeated, and pointed feedback made clear that was the wrong fix: seeing "unverified" callouts show up in generated SOPs at all was itself the problem, not just what they contained, and a real sysadmin-authored reference SOP was provided showing the actual target — clean, confident, zero hedging anywhere, with a specific formal section structure (Purpose, Scope, Prerequisites, a pre-execution checklist, the procedure itself, post-execution validation, rollback and escalation). `SOP_SYSTEM_PROMPT` and `REVIEW_IMPROVE_SYSTEM_PROMPT` now: (1) require that same seven-section structure, retitled per procedure (e.g. "Pre-Deployment Checklist"); (2) forbid hedging/warning-style callouts entirely — when genuinely unsure of one exact vendor-specific detail, the rule is to use safe, standard, verifiably-correct phrasing instead of either inventing a specific fact or flagging the gap, not to state a guess as fact either; (3) require matching the task's real interaction mode (GUI menu path, physical/manual action, or CLI) instead of defaulting to CLI/PowerShell commands for procedures that are actually GUI-only — a second real complaint reported alongside the first. Review & Improve additionally strips any pre-existing `[!WARNING]`-style hedge it finds in a document being improved, rewriting the affected step as confident, safe-phrased prose rather than just deleting the warning and leaving the flagged guess behind. `lib/sop/remarkGithubAlerts.ts`'s rendering support for GitHub-style alert blockquotes is left in place (it's generic Markdown rendering, not exclusively tied to the removed behavior — still renders correctly if you hand-write or import a `[!WARNING]` block yourself), but nothing in generation produces one anymore. Verified against a real local model (Ollama) on three fronts: a Windows-server-restart topic came back in the full seven-section shape with zero hedging and, after one further prompt tightening pass, the actual GUI restart sequence (Start > Power > Restart) instead of a `shutdown` CLI command it had reached for on the first attempt; a repeat of the original RiseVision scenario that motivated the old warning-flag feature came back with no `[!WARNING]` and, just as important, without regressing into confidently inventing specific-sounding facts either — it described RiseVision's real management console and a generic "Reset" button rather than a fabricated systemd service name and config path. Also caught along the way: the new, considerably longer system prompt combined with the full JSON Schema (which the Ollama adapter embeds directly in-prompt) was pushing close to a small local model's context window, causing requests to balloon to 15+ minutes and eventually time out — fixed by trimming redundant prose across `SOP_SYSTEM_PROMPT` and several `schema.ts` field descriptions (the same rules stated once, prominently, instead of restated three different ways) without losing any of the substance. A follow-up report caught a second gap in the same feature: the seven sections came back as bare, unnumbered headings (`## Purpose`, not `## 1. Purpose`), and on one generation as a bare document with no `#` title at all — the reference SOP's actual outline used numbered section headings (`1. Purpose`, `2. Scope`, ...) as real structure, not just plain prose titles. `SOP_SYSTEM_PROMPT` now spells out the heading levels explicitly rather than leaving them to be inferred from the `##` glyphs in its own examples: the first line of template_markdown is always a single level-1 `#` document title, and each of the seven sections below it is a level-2 `##` heading with its number written into the heading text (`## 1. Purpose`). Verified against a real local model after the fix: `# Restart Production Windows Server After Approved Maintenance` followed by `## 1. Purpose` through `## 7. Rollback and Escalation`, matching the reference SOP's outline exactly.
- **Bolded UI elements in GUI steps.** A GUI-driven step naming the things you actually click — "Navigate to Organization > Inventory in the left navigation menu" — used to come back as plain prose with no visual distinction between the click targets and the surrounding sentence. `SOP_SYSTEM_PROMPT`/`REVIEW_IMPROVE_SYSTEM_PROMPT`/`REFINE_SYSTEM_PROMPT` now require bolding every UI element the operator clicks or types into — a button, menu item, tab, link, or field name — one bold span per element, including each part of a multi-step navigation path (`Navigate to **Organization** > **Inventory**`, not the whole sentence or generic surrounding words). Reinforced in `template_markdown`'s JSON-Schema description in `schema.ts` too, same reinforcement pattern used for the other generation-quality rules. Verified against a real local model (Ollama): a Meraki switch-claiming SOP came back with `**Organization**`, `**Inventory**`, `**Claim a New Device**`, and other real UI element/field names consistently bolded throughout every GUI step, not just the first one — the model does vary between bolding a multi-segment path as one span (`**Organization > Inventory**`) versus per-segment (`**Organization** > **Inventory**`) from one occurrence to the next, an accepted minor stylistic inconsistency from this particular small local model rather than something worth another prompt-tuning pass, since either reading is still scannable and correct.
- **No fabricated identifiers, and no fields for values you'll never actually type in.** Reported live from a real generated SOP: a Meraki device-claim procedure stated a specific setup URL (`http://setup.meraki.com`) as settled fact, and separately the model was inventing plausible-looking serial numbers, device names, and IPs — values that don't have a genuine "typical" default the way a port or protocol does, and a fabricated one that *looks* real is worse than an obviously empty field, since it can be mistaken for a real value and quietly ends up wrong. `SOP_SYSTEM_PROMPT`/`REVIEW_IMPROVE_SYSTEM_PROMPT`/`IMPORT_ANALYSIS_SYSTEM_PROMPT` draw the line on *when* a value is known, not just what kind it is: only parameterize what the person adapting the SOP would decide **before** running it (a target network name, a VLAN ID, a domain); never parameterize what's only discovered **during** the procedure (a serial number read off a label, a MAC address, a DHCP-assigned IP/gateway, an activation code generated on the spot) — those stay plain instructional prose with no `{{}}` at all, reinforced with an explicit right/wrong example in both the system prompt and `schema.ts`'s `key`/`default` field descriptions. Verified against a real local model (Ollama) on the exact reported scenario (claiming a Meraki switch): the DHCP-assigned management IP stopped being parameterized entirely on the retest, though the same small model still parameterized the serial number despite an example matching that literal case — consistent with this particular quantized local model's uneven instruction-following seen elsewhere in this project; a stronger hosted model should hold the line more consistently, and either way there's a manual backstop (see the next bullet) for whatever the AI still gets wrong. An empty declared variable used to render as a `[Field Label]` bracket hint inline in the sentence ("...at [Site Name]...") — reported back as actively unwanted, since a lot of real usage here is one SOP template reused across many different sites/devices, where most fields staying unfilled indefinitely is completely normal, not an error to flag every time the document is viewed. The literal `{{key}}` is left in place instead (`lib/sop/template.ts#renderTemplate`, `lib/sop/remarkSubstituteVariables.ts` for the live preview), reading unambiguously as a template slot rather than broken English prose, still marked with the same `unbound-placeholder` amber styling an unrecognized `{{key}}` already had so it isn't literally invisible either.
- **Remove Field, now with an actual reword** — hovering any variable in the form always shows an X (every field is removable, not just ones you added yourself — see the `customKeys` removal below). Removal used to be a purely mechanical local substitution (drop `{{key}}`, splice in its current literal value or delete it if empty) — fast, but it left an awkward gap or leftover value wherever the field was referenced, since nothing actually reworded the sentence around it. Reported back as not good enough: clicking remove should make the document read as if that field had never existed. Removal now goes through the same multi-turn Refine pipeline as any other edit instruction (`SopWorkspace.tsx#handleRemoveField` calls `handleRefine` with a synthesized instruction), so the AI rewrites every affected sentence into genuinely natural prose — slower (a real AI round trip, shown with a spinner on that field's X and the whole form locked meanwhile, `VariableForm.tsx`'s new `disabled`/`removingKey` props) but the field is actually gone from the prose, not just gone from the form. Caught a real instruction-following miss while verifying this against a real local model (Ollama): the first attempt "removed" `{{switch_serial_number}}` by literally swapping the braces for a bracket — `Enter the Serial Number: [switch_serial_number] and click Continue.` — which is the exact broken-text failure mode this feature exists to avoid, just with different punctuation. Fixed by adding an explicit right/wrong example to both `REFINE_SYSTEM_PROMPT` and the synthesized removal instruction itself (using the real field name so the example is concrete, not generic); retested and the same field now correctly becomes `Enter the Serial Number and click Continue.` — a normal sentence, no braces, brackets, or variable name anywhere. This retired the `customKeys` bookkeeping (a `Set<string>` tracking which fields were user-added, threaded through state, Library records, and `VariableForm.tsx`'s render gate) entirely, since with every field removable it had no remaining purpose.
- **Guided generation** — a `?`-icon button next to the topic box (enabled once you've typed a topic) for when you know roughly what you need an SOP for but not how to break it into the right steps or what details even matter, the exact problem a blank "describe the task" box doesn't help with. Clicking it calls `app/api/clarify/route.ts` (`CLARIFYING_QUESTIONS_SYSTEM_PROMPT` in `lib/llm/prompt.ts`), which asks the AI what a technical writer would actually need to know before writing this specific SOP well — vendor/platform, environment, existing conventions, risk/rollback expectations — rather than generic filler, typically 3-6 questions, each with a short example placeholder. `GuidedQuestionsDialog.tsx` shows them as a short form; "Skip & generate now" falls back to a normal Generate with just the topic, and submitting answers calls the same `/api/generate` as always with a new `clarifications` field, appended to the prompt as authoritative facts about this specific setup (`buildUserPrompt`'s new block, same mechanism as category profile context) — a blank answer is simply omitted rather than sent as "no answer." Verified against a real local model (Ollama) end-to-end, not just that questions come back: asked to reset a locked-out user account, it first asked exactly the right things (identity platform, on-prem vs. cloud, access method, ticketing system, rollback expectations); answering Okta/cloud/Jira Service Management then produced an SOP that actually used the real Okta Admin Console flow and referenced Jira Service Management for the ticket, not a generic "your identity provider" placeholder — concrete evidence the answers are actually shaping generation, not just being appended and ignored.
- **Category profiles** — a local, reusable "here's what I already know about this kind of SOP" per category (e.g. *User Reset*, *Meraki Networking*), so you don't have to re-explain the same environment facts (an AD domain, a ticketing system, a standard management VLAN) or re-type the same default values every time you generate in that category. Typing a category into the new field next to the topic box (`TopicInput.tsx`, autocompleted from `categorySuggestions` — every category with a saved profile) does two things on the next Generate: the profile's free-text `context` is sent alongside the topic as grounding (same mechanism as Attach Reference — `buildUserPrompt`'s new `categoryProfile` block in `lib/llm/prompt.ts`, threaded through `app/api/generate/route.ts`), and any variable the AI declares whose key matches a remembered default gets that value pre-filled client-side instead of the AI's own default. The profile *builds itself* rather than requiring a form to fill out up front: clicking Save to Library diffs each field's current value against the AI's original default, and — only when at least one genuinely changed — proposes the changed ones as new/updated profile defaults, auto-opening `CategoryProfilePanel.tsx` with them pre-checked next to the free-text context box; nothing is written to the profile until you review and click "Save profile" there (or "Cancel" to skip it for this save). The same panel, opened manually via the book icon next to the category field, is also the plain viewer/editor/delete UI for a category's profile at any time, whether or not there's anything pending review. Storage is `lib/sop/categoryProfiles.ts` — plain IndexedDB, no network code path at all, same guarantee as the Library (they now share one database via the new `lib/sop/db.ts`, which centralizes the store definitions so two modules can't independently version/upgrade the same IndexedDB database against each other). Caught and fixed one real bug while wiring the panel's Save button: it was rendered as a `<form>` nested inside `TopicInput`'s own `<form>` — invalid HTML — and clicking Save triggered a native full-page form submission that wiped all in-memory state, reproduced live with Playwright (the whole app reset to its pre-generation empty state) before being fixed by making the panel a plain `<div>` with a regular button instead. Verified end-to-end with a mocked-response Playwright session: a category typed before Generate is sent as `categoryProfile.category`; a changed field value survives Save to Library → review panel → Save profile → a second Generate in the same category, arriving pre-filled without being typed again, and the saved context string is confirmed present in that second request's body.
- **Preset categories, and an actually-fixable category on a generated SOP.** Two gaps reported together: before you'd saved a single category profile, the category field had nothing to suggest at all — an empty box with no sense of what naming scheme to settle on; and the AI-assigned category on a just-generated SOP, while technically always editable, lived in a tiny unlabeled uppercase badge in the sidebar with no visual hint it was an input at all, let alone one with anything to pick from. `lib/sop/presetCategories.ts` adds ten common starter categories (Networking, SysAdmin, Security, User Management, Hardware, Software & Applications, Backup & Recovery, Cloud & DevOps, Facilities & Office, Onboarding & Offboarding); `SopWorkspace.tsx` merges them with every category you've actually saved a profile for into one case-insensitively deduped, alphabetized suggestion list, and both the pre-generation category field (`TopicInput.tsx`) and the post-generation sidebar badge now share the exact same `<datalist>` — previously only the pre-generation one had suggestions wired up at all. Typing over the sidebar badge still works exactly as before (it was already a real `<input>`, just not an obviously interactive-looking one); this only adds the dropdown of options and a clearer tooltip. Changing it there updates the in-session document immediately, same as always — click Save to Library (or "Update Saved Copy") afterward to persist the correction to a saved record. Verified live with Playwright: the datalist carries all ten presets before any generation has happened, and a mock "Wrong Category" response can be corrected by picking or typing directly in the sidebar field.
- **Refine with AI** (chat-bubble icon in ActionBar) is a multi-turn "tell it what to change" session, for edits Review & Improve's fixed quality checklist doesn't cover — "this is for a Linux server, not Windows," "add a DNS check before the reboot step," "move that verification step earlier." There's no real multi-turn chat state on the wire — no adapter/provider changes were needed — each submission just resends the current document (freshly rendered, so it reflects any manual field/Source edits made between turns) plus every prior instruction from this session, in order (`REFINE_SYSTEM_PROMPT`/`buildRefinePrompt` in `lib/llm/prompt.ts`, `app/api/refine/route.ts`), which gives the model everything it needs to behave like it remembers the conversation — a later instruction ("also move that check before the reboot step") correctly builds on an earlier one without you restating context. `REFINE_SYSTEM_PROMPT` holds it to applying only the new instruction (not re-litigating earlier ones), fixing whatever else the change makes inconsistent, and preserving an existing variable's current value as its default rather than replacing it with a fresh generic example — otherwise every turn would silently wipe whatever you'd already typed into the form. `RefinePanel.tsx` shows the running list of applied instructions and stays open across multiple submissions (unlike Guided's one-shot dialog) since the whole point is quick back-and-forth; a standing privacy notice replaces the `window.confirm` Scan with AI/Review & Improve use, since confirming every single message would defeat that. History resets on anything that replaces the whole document (Generate, Regenerate, Import, Start Blank, a Library load) and persists across in-place edits (Scan with AI, Review & Improve), same split `libraryId` already uses. Unlike `CategoryProfilePanel`'s self-contained trigger+popover (a design forced by an earlier nested-`<form>` bug — see above), the trigger button here lives separately in `ActionBar.tsx` and the panel itself is a full-screen modal (`fixed inset-0`), so there's no shared-container click-outside concern to work around. Verified live with Playwright that the panel correctly stays open across multiple submissions, shows the growing instruction history, and resets on Start Blank + regenerate. Verified against a real local model (Ollama) end-to-end: told to change a Windows restart SOP to a Linux one and use SSH instead of RDP, the result updated the title, every RDP reference, the restart step (a GUI `Start > Power > Restart` sequence became `sudo shutdown -r now`, correctly matching how Linux administration actually happens), and even a command-equivalent detail (`query user` → `who`) it wasn't explicitly told about — while the sections the instruction didn't touch (Post-Restart Validation, Rollback and Escalation) came back unchanged.
- **Library** (icon in the header, "Save to Library"/category input above the variable form) is a local SOP collection — browse, search by title/overview, filter by category, load, delete. Both the title and category next to a generated/loaded/hand-written SOP are directly editable inputs (previously only category existed, and title was a static label that never reflected hand-edited content — a real gap found while testing this: after Start Blank + hand-writing, every save landed as "Untitled SOP" because nothing derived a title from the template content or let you set one, which defeats the point of a browsable library). Saving assigns a `crypto.randomUUID()`; saving again from the same loaded/just-saved document updates that record (button label changes to "Update Saved Copy") rather than duplicating it. Any action that fully *replaces* the document (Generate, Regenerate, Import, Start Blank — the "New" button in the topic bar, always visible so a second blank document is reachable mid-session, not just from the pre-first-SOP empty state) drops the link to the previous library entry, since silently overwriting a saved record via an unrelated action would be a bad surprise; AI-assisted *edits in place* (Scan with AI, Review & Improve) keep it. See "Privacy & data handling" above for how the isolation you'd expect from a "local library" is actually enforced (not just promised): `lib/sop/library.ts` has no code path to the network at all.
- **Automatic secret redaction** (`lib/sop/redactSecrets.ts`) runs on the outgoing copy of anything sent to the AI for Scan with AI, Review & Improve, and Attach Reference (at attach time, not just send time, since an attached file's whole purpose is to be sent) — never on your own local editor/topic state, which is left exactly as you typed it. Detects private-key blocks, well-known vendor token prefixes (AWS, GitHub, Slack, Stripe, Google, OpenAI, SendGrid, JWTs), credentials embedded in `scheme://user:pass@host` connection strings, and `KEY: value`/`KEY=value` config-style declarations where the key name itself signals a secret (password, api_key, token, etc.) — matched with an explicit assignment operator required right after the label, so prose that merely *mentions* "password" or "token" conceptually is left untouched. Caught a real bug while verifying this: the labeled-declaration pattern originally required a strict regex word boundary before the label, which doesn't fire between `_` and a letter (both are word characters) — so extremely common `DB_PASSWORD=...`/`MYSQL_PWD=...`-style env-var naming slipped through untouched entirely. Fixed by absorbing surrounding identifier characters into the match instead of requiring a hard boundary. Verified with a dedicated unit test (private keys, six vendor token shapes, a credential URL, and `DB_PASSWORD`-style labels all redacted; unrelated content and conceptual prose mentioning "token"/"password" left alone) and live in a browser: attached a file with a real-shaped password and API key, confirmed the UI shows a "(N redacted)" badge, and intercepted the actual outgoing `/api/generate` request to confirm the raw secret values never appear in it.
- **Review & Improve** (ActionBar) is Scan with AI's counterpart with a different constraint: Scan with AI must preserve wording exactly and only insert `{{key}}` placeholders; Review & Improve (`app/api/review-improve/route.ts`, `REVIEW_IMPROVE_SYSTEM_PROMPT` in `lib/llm/prompt.ts`) is explicitly allowed to rewrite or add content to fix real problems in an existing SOP — the same quality bar `SOP_SYSTEM_PROMPT` holds fresh generation to (fix a hardcoded value that should track a variable, turn rollback pseudocode into real commands, add a missing pre-flight safety checkpoint) applied to something a human wrote, pasted in, or hand-edited, while trying to preserve the author's actual structure and steps. Same deliberate "full document, redacted first, confirmed every time" pattern as Scan with AI. Verified live against a real local model (Ollama) with a deliberately flawed hand-written SOP (a port hardcoded to "HTTPS" regardless of the port variable, a bracketed pseudocode rollback step, no safety checkpoint despite being a firewall change): it added an explicit backup-verification prerequisite and replaced the pseudocode rollback with an actual command, but did *not* catch the HTTPS/port coupling issue on this attempt — consistent with this particular (small, local) model's uneven performance throughout testing, and reported here honestly rather than claimed as a clean 3-for-3, since a review pass finding a subtle existing flaw is a harder task for a weak model than following the same rule while writing fresh.
- **Start Blank** creates an empty document and drops you straight into the Source tab — no AI call, nothing pre-filled beyond a single `# Untitled SOP` heading. Reachable two ways: the empty-state link (before anything's loaded) and the always-visible "New" button in the topic bar (see the Library bullet above for why the always-visible one had to be added). Exists so hand-writing an SOP from scratch (using the formatting toolbar) and then running Review & Improve on it doesn't require going through Generate or Import first.
- **Suggest Ideas** ("Suggest Ideas" button, appears next to attached files once at least one is attached) proposes a list of SOP topics grounded in the attached documentation, rather than requiring you to already know what to generate — the "give it a manual, get SOP ideas back" flow. Reuses Attach Reference's existing files/redaction, no separate attach step. Required a real architectural fix, not just a new prompt: every `LlmAdapter` implementation (OpenAI/Gemini/Ollama) had the single-SOP JSON Schema hardcoded internally, so even with a correct new prompt asking for a list of ideas, the provider's own structured-output mechanism (OpenAI function-calling parameters, Gemini `responseSchema`, Ollama's schema-in-prompt) would have silently forced the response into the wrong shape regardless of what was asked. `generate()` now takes the schema as a parameter (`sopJsonSchema` for the existing three routes, a new `sopIdeasJsonSchema` for this one) instead of each adapter assuming one fixed shape. Picking a suggestion pre-fills the topic box — which surfaced a second, unrelated real bug while wiring this up: `TopicInput`'s `initialValue` prop was only ever consumed by `useState` on the component's first render, so setting the topic from outside after mount (Import's derived title, a Library load, and now this) silently never updated the visible input, even though the underlying state was correct (Regenerate uses that state directly, so it wasn't broken, just invisible) — fixed with a `useEffect` that resyncs on prop change, verified live that Import/Load/pick-an-idea now all show up immediately. Verified the ideas feature itself against a real local model (Ollama): a fictional "Zorbnet CLI" README describing six distinct commands (`init`, `grant-access`, `snapshot`, `rotate-creds`, `device add`, `device retire`) produced exactly one grounded idea per command, each justified by referencing the actual command — not generic filler, and not a wrong-shaped response, confirming the schema-threading fix actually works end-to-end through the adapter that needed the most rework (Ollama, whose "schema" is just prompt text, not an enforced API parameter).
- **Attach Reference** (paperclip icon next to Generate) lets you attach one or more local files (README, source, config — `.md`/`.docx` go through the same reader Import uses via `lib/sop/readFileAsText.ts`; anything else is read as plain text, so source/config files work directly) as ground-truth material for *generation*, not just topic text. Built for internal or non-public programs the AI has no other knowledge of: the topic says what to do, the attachment(s) say how the specific tool actually behaves. `buildUserPrompt` in `lib/llm/prompt.ts` appends the attachments as a clearly delimited "Reference material" block, and `SOP_SYSTEM_PROMPT` instructs the model to treat that block as authoritative — prefer its facts/commands/syntax over generic assumptions, and don't invent behavior it doesn't support. Capped client-side and re-enforced server-side (`lib/sop/contextLimits.ts`, `app/api/generate/route.ts`) at 10 files / 150,000 characters total. Attached files persist across Regenerate (same topic, same grounding) but clear on Import (an unrelated flow). Verified two ways: a Playwright session confirmed the outgoing request actually carries the attached content and that the file-count cap is enforced when attaching many files in one picker action — a real bug here, caught by that same test: the per-file handler checked `contextFiles.length` against React state that hadn't updated yet partway through a multi-file batch (each call in the loop saw the same stale pre-update count), letting 11 files through a 10-file cap; fixed by processing a whole file-picker selection as one batch against a single state read instead of one closure per file. Then verified the actual grounding effect against a real local model (Ollama): wrote a README for a fictional "Zorbnet CLI" tool with an invented `--level glorp` access flag that exists nowhere in the model's training data, attached it, and asked for an access-grant SOP — the model used the exact fictional command syntax and the made-up "glorp" level rather than inventing a generic "grant admin access" procedure, and even respected the README's explicit "there is no admin/read-only concept" constraint.
- **Import** (upload icon next to Generate) loads a local `.md`/`.markdown`/`.txt`/`.docx` file straight into the same editing UI a generation produces. Two layers of variable detection, both explained in more detail below: `lib/sop/detectVariables.ts` runs automatically, entirely locally (IPs, MAC addresses, emails, `<ALL_CAPS>` placeholder tokens, `____` blanks, `TBD`/`TODO`/etc. all get turned into `{{key}}` fields with the original value as the default), and the **Scan with AI** button is an explicit opt-in for a smarter pass that actually reads the document. The title is the file's first `# ` heading (falling back to the filename), and any `{{key}}` placeholders already in the file (from either detection layer, or already present in the source) are turned into editable fields via the same logic `handleTemplateChange` uses for hand-edited Source content. Lets you take a previously-exported (or hand-written) SOP and keep editing it. `.docx` files go through `lib/sop/docxToMarkdown.ts` (mammoth unzips the OOXML into HTML, `turndown`+GFM plugin turns that into the same markdown dialect the app renders) — importing a raw `.docx` used to just dump its zip bytes as text (`file.text()` on a binary file), caught from a real screenshot of the resulting garbage. To recover formatting a generic docx reader can't infer from direct formatting alone, `markdownToDocx.ts` now writes inline code / code blocks / blockquotes as named Word styles ("SOP Inline Code" etc.), which `docxToMarkdown.ts` maps back via a custom mammoth `styleMap`; documents from other tools (or older exports without these styles) still import fine, just without that extra fidelity. Verified round-trip via a compiled-standalone Node harness (mammoth's Node build + a `domino`-backed `DOMParser` shim) parsed back through the app's actual remark/remark-gfm stack: headings, bold/italic, inline code, fenced code blocks (multi-line — an early attempt silently dropped every line after the first because of how turndown reads `<pre><code>` content), GFM tables (mammoth emits plain `<td>` with no header signal, which makes turndown-plugin-gfm refuse to convert the table at all and fall back to raw un-rendered HTML — fixed by promoting row 0 to `<th>`; a second bug found the same way — cell content came back with embedded raw newlines from `<p>`-wrapped cells, which breaks GFM's one-line-per-row requirement — fixed with a custom cell rule), blockquotes, and embedded images all confirmed to parse as the correct AST node types, not just "looks similar" text. Shipped once with a real bug reported live from an actual export-then-reimport: turndown's default escaping backslash-escapes characters that could be ambiguous in *generic* markdown output — `{{site_name}}` (a plain variable key, always snake_case) came back as `{{site\_name}}`, and `## 1. Purpose` as `## 1\. Purpose`. The escaped backslash falls outside `PLACEHOLDER_RE`'s `[a-zA-Z0-9_]*` character class, so the placeholder silently stopped matching at all — re-importing a document this app had itself just exported lost every variable, landing on an empty Variables panel despite the `{{key}}` text still visibly sitting in the rendered document. Fixed with `turndownService.escape = (text) => text`, disabling that escaping entirely — safe here since this file's whole job is one specific round trip back into the exact dialect this app's own remark pipeline already parses deterministically, not producing portable markdown for an arbitrary downstream renderer. Verified live: export a generated SOP with unfilled `{{key}}` variables and numbered headings, Start Blank, re-import that exact file — the Variables panel now repopulates correctly and the Source tab shows `{{site_name}}`/`## 1. Purpose` with no stray backslashes.
- **Local variable detection** (`lib/sop/detectVariables.ts`) is deliberately conservative — high-confidence patterns only (IPv4, MAC, email, `<ALL_CAPS>` bracket tokens, `TBD`/`TODO`/`FIXME`/etc., `____` blank-fills), not a generic "anything bracketed" rule, since a broad bracket pattern would also catch markdown link text (`[label](url)`) and task-list checkboxes (`[ ]`/`[x]`). Same literal value found more than once maps to the same `{{key}}` everywhere it appears; the substitution pass sorts matches longest-first before a single combined-regex replace so one match can't corrupt another that's a substring of it (e.g. IP `10.0.0.1` inside `10.0.0.10`) — verified with exactly that case. Verified end-to-end with a real Playwright browser session (not just the pure function in isolation): imported a doc with an IP/email/bracket-token/blank-fill/pre-existing `{{key}}`, confirmed each became an editable field pre-filled with its real detected value (not blank), confirmed the rendered preview showed the real values rather than raw `{{key}}` text, and confirmed markdown links and GFM task-list checkboxes were left untouched.
- **Scan with AI** (ActionBar, `app/api/analyze-import/route.ts`) sends the imported document's full rendered content to the configured AI provider, which reads it and does the same job the local pass does but with actual understanding instead of regex — real judgment about which values are site/user-specific, not just ones that happen to look like an IP or an email. Reuses the exact same schema/parsing/reconciliation pipeline as `/api/generate` (`lib/llm/schema.ts`, `lib/sop/parseJson.ts`, `lib/sop/reconcile.ts`) with a different prompt (`IMPORT_ANALYSIS_SYSTEM_PROMPT` in `lib/llm/prompt.ts`) instructing the model to preserve wording/structure exactly and only insert `{{key}}` placeholders, with `variables[].default` set to the original value so the document still renders unchanged until you edit a field. This is the one deliberate exception to "only the topic is ever sent to the AI" (see "Privacy & data handling" above) — `window.confirm` spells that out before every call, no "don't ask again."
- **Formatting toolbar** (`MarkdownToolbar.tsx`) — Bold/Italic/Inline code/Strikethrough/Heading 1-3/Bullet & numbered list/Blockquote/Task list/Table/Code block/Link/Horizontal rule buttons that operate on the current selection (wrap it, prefix each selected line, insert a snippet) rather than a separate rich-text document model, so there's no markdown<->rich-doc conversion to keep in sync with the rest of the app. Icon-only with a `title` tooltip, same as ActionBar. List/heading/task-list buttons toggle off if the selection is already formatted that way. Sits above **both** editors now (see "Live editable Rendered view" below) via an `EditorAdapter` interface (`getSelection`/`setSelection`/`focus`) — the toolbar's actual formatting logic never changed, only how it reads/writes the selection, so the exact same buttons work whether you're in Source (a plain textarea) or Rendered (CodeMirror). Verified with a standalone unit-test harness against the actual compiled functions (not reimplemented copies) — caught a real bug this way: numbered-list numbering skipped a number whenever the selection included a blank line, because the line index used for numbering counted blank lines too; fixed by only advancing the counter on non-blank lines. Also verified live in a browser: selecting text and clicking Bold/Heading 1/Table produced the expected markdown and rendered correctly afterward, and separately confirmed Strikethrough/Task list/Horizontal rule each produce the real corresponding element (`<del>`, a checkbox `<input>`, `<hr>`) rather than literal text.
- **Live editable Rendered view** — the Rendered tab used to be read-only (a plain `react-markdown` render); reported as a real gap for anyone hand-authoring an SOP without AI at all — Start Blank dropped you into raw markdown syntax with no visual feedback until you switched tabs. It's now a genuine live-preview editor: type `**bold**` and it renders bold *as you type*, `# ` becomes a real heading size, `` `code` `` gets shaded, `> [!WARNING]` becomes the same colored callout the old preview gave it — all while the document stays exactly what it's always been, a plain markdown string (`template` state), because the editing surface is CodeMirror 6, not a rich-text/AST framework like TipTap or ProseMirror. That choice was deliberate: this app's export/copy/AI pipeline all treat that markdown string as the single source of truth, and a rich-text editor would mean a second document model with a lossy round trip to keep in sync; CodeMirM6 just *decorates* the same string live instead of replacing it. Deliberately out of scope for this pass: hiding markdown syntax characters entirely while the cursor is elsewhere (the "Obsidian mode" where `**bold**` collapses to just a bold "bold") — markers are dimmed instead of hidden, needing far less cursor-position-dependent decoration-invalidation logic that could silently corrupt the document if a hide/show transition got the position wrong; tables/images/links render as plain (readable, syntax-highlighted) text with no interactive grid or embed. `{{key}}` variables render as the exact same substituted chip the old preview used — reusing its `sop-var-value`/`unbound-placeholder` CSS classes and `data-sop-var` attribute directly, so hover-to-locate needed no new logic, just pointing at a different scroll container (`lib/sop/liveMarkdownExtensions.ts`'s `VariableChipWidget`) — and are atomic from the cursor's perspective (`EditorView.atomicRanges`), so you can't type into the middle of one and corrupt the key; deleting one removes the whole `{{key}}` token, not a character at a time. Caught and fixed two real bugs while building this: (1) the decoration plugin crashed on every render with "Ranges must be added sorted by `from` position" — a hand-merged single `RangeSetBuilder` mixing zero-width line decorations (heading size, blockquote background) with variable-width mark decorations (bold, dimmed markers) from a different collection violated CodeMirror's strict ordering requirement; fixed by splitting them into two separate `ViewPlugin`s and letting CodeMirror's own facet combination merge them, the documented way multiple decoration sources are meant to coexist. (2) `#print-target` (what Export PDF/print actually captures) used to only exist in the DOM while the Rendered tab was the active one, since it and the editable view were the same conditional branch; now that Rendered is a *different*, editable component, `#print-target` had to become permanently mounted (zero-footprint, hidden via a `visibility`/`position` trick that a higher-specificity `@media print` ID selector already correctly overrides) so printing keeps working regardless of which tab is showing — verified live that it holds the correct fully-substituted content and stays invisible/zero-size in both tabs during normal use. Verified end-to-end with Playwright (13 checks): heading/bold/italic/code/GFM-alert decoration classes and the alert label widget all appear; a filled variable chip shows its value and an unfilled one shows the raw `{{key}}` with amber styling; typing in the live editor propagates to the underlying template and survives a round trip through the Source tab; the shared toolbar's Bold button works against the live editor through the new adapter; and editing a field's value in the sidebar updates its chip live without touching anything else on the page. Reported back with a screenshot within hours of shipping: the gap between a heading and its body text looked noticeably larger than the old preview ever had, and the app itself had grown an outer scrollbar it never used to. Root cause — a blank markdown line between a heading and a paragraph is meaningful structure (the paragraph break), but CodeMirror renders it as one full line-height of literal empty space by default, where the old `react-markdown` render turned that same blank line into a heading's small CSS margin (`.sop-prose h2 { mb-2 }`, ~8px) instead; stacked across a full seven-section SOP's worth of blank lines, the difference was enough to push the whole page past the viewport height. Fixed by detecting blank lines during the same syntax-tree walk that builds heading/blockquote line decorations and giving them their own `cm-sop-blank-line` class (tiny font-size/line-height, collapsing them to roughly a heading's old margin) rather than shrinking the base line-height globally, which would have made actual text cramped too. Verified live: the gap between a heading line and the paragraph right after it went from 27px to ~4px, and a 3-section test document that previously overflowed an 860px-tall viewport (860 vs. an 884px document — scrolling) now fits with room to spare (860 vs. 860, no scroll) at that same size.
- **ActionBar is icon-only** — every button (Regenerate, Add Custom Field, Scan with AI, Review & Improve, Insert Image, Copy) shows just its icon with the label as a `title` tooltip instead of visible text, and the three export actions (.md/.docx/PDF) are collapsed into one "Export" button with a dropdown (`ExportMenu`, inside `ActionBar.tsx`) rather than three separate buttons — both purely to fit a now-fairly-long action list into one row without wrapping. The dropdown items themselves keep visible text labels (a dropdown menu, unlike a toolbar, isn't space-constrained the same way, and item text is the normal way to identify a menu action). Reuses the same `useOnClickOutside` hook as every other popover in the app. Verified live: every button's visible text is empty (icon-only) with the correct tooltip, the dropdown opens/closes correctly (including via outside-click), and clicking a dropdown item triggers a real file download and closes the menu.
- **`{{key}}` substitution also handles variables wrapped in backticks** — a generated SOP wrapping a placeholder in code styling (`` `{{display_name}}` ``, since models reach for code formatting on anything that looks like a technical value) used to leave that specific occurrence permanently un-substituted: reported live from a real generated SOP where every variable happened to be backtick-wrapped, so none of them showed the actual field value or updated when edited, staying stuck as literal `{{key}}` text in a code box — while identical variables elsewhere in the same document worked fine. Root cause: `remarkSubstituteVariables.ts` only ever visited mdast `text` nodes; a backtick span parses as a separate `inlineCode` node type whose raw content it never looked at. Fixed by adding a matching visitor for `inlineCode`, including the mixed case (a variable embedded inside a larger code span, e.g. `` `ping -c 2 {{player_ip_address}}` ``) — the non-variable portion keeps its code styling, the variable portion becomes a real substituted, hoverable `data-sop-var` span like anywhere else. Verified against the exact reported scenario, not a simplified stand-in: a real Playwright session confirmed a backtick-wrapped placeholder shows the real field value, updates live when the field is edited, is a genuine `data-sop-var` span (hover-to-locate works), and that the mixed-span case correctly splits code styling from the substituted value — while a genuine non-variable code span (e.g. a keyboard shortcut) elsewhere in the same document is left completely untouched.
- **Hover-to-locate**: hovering a field in the left Variables panel highlights and scrolls to its occurrence(s) in the Rendered view; hovering a value there always shows a tooltip naming which field it came from. Both the read-only print-target render and the live editor's variable chips use the same `data-sop-var="key"` attribute and `sop-var-value`/`unbound-placeholder` CSS classes (`lib/sop/remarkSubstituteVariables.ts` for the former — a custom remark plugin substituting `{{key}}` at the mdast AST level via `unist-util-visit` + mdast-util-to-hast's `data.hName`/`hProperties` convention rather than injecting an HTML string, since field values are user/import-controlled and `rehype-raw` would be a real injection surface; `lib/sop/liveMarkdownExtensions.ts`'s `VariableChipWidget` for the latter), so the hover-highlight `useEffect` in `MarkdownPreview.tsx` needs no branching between them beyond which DOM container it queries. The scroll+highlight direction can be turned off from the sliders icon in the header (`PreferencesPanel.tsx`, localStorage-backed, works in both self-hosted and desktop); the tooltip direction is always on since it's inert until hovered. Caught a real bug while verifying this live in a browser: the highlight-and-scroll effect originally used `Element.scrollIntoView()`, which per spec can scroll *every* scrollable ancestor to bring an element into view, not just the nearest one — reproduced with a Playwright session and a `MutationObserver`/scroll-event logger, it was nudging the whole page (including the left Variables panel, ~35-65px) while the mouse stayed at fixed screen coordinates, so mid-hover a *different* field row would end up under the cursor and the highlight would jump to the wrong field. Fixed by computing the target's offset and calling `scrollBy()` directly on the hovered view's own scroll container, which can only ever affect that one container; reverified the left pane's row positions stay pixel-identical throughout the scroll.
- **Every popover closes on an outside click** (Preferences, the update panels, AI provider settings, Add Custom Field) via a shared `lib/hooks/useOnClickOutside.ts` — previously each stayed open until its own trigger button was clicked again. Listens on `pointerdown` rather than `click` and checks `element.contains(e.target)`, so a click on the trigger button itself (which lives inside the same wrapped container as the popover) is correctly treated as "inside" and doesn't fight with the button's own open/close toggle. Verified live for three of the five (Preferences, the self-hosted git UpdatePanel, Add Custom Field): opens on click, stays open when clicking inside its own content (including while typing into a field, confirming it doesn't lose a half-filled form), closes on an outside click without submitting anything, and the trigger button still toggles it closed normally.
- **Add Custom Field** adds a form field immediately; paste `{{your_key}}` into the Source tab to wire it into the document. Conversely, typing a new `{{key}}` directly into Source auto-creates its form field — the two directions are kept in sync in `SopWorkspace.tsx#handleTemplateChange`.
- **Export PDF**: on desktop, `electron/main.js`'s `export:pdf` IPC handler calls `webContents.printToPDF()` directly and writes the buffer to a file the user picks via a native save dialog — a real, text-searchable PDF, not a screenshot. Self-hosted (no `window.electronAPI`) falls back to the browser's native print dialog. Both render against the same `#print-target`-scoped print stylesheet in `app/globals.css`, which explicitly overrides the app's dark theme to a light/print-appropriate palette *and* cancels the live element's `h-full overflow-y-auto` scroll-container sizing so long content actually paginates instead of cropping to whatever fit in the visible scrolled viewport — reproduced live twice: first as the dark theme bleeding through (white-on-black), then — after fixing only the colors — as a real multi-section SOP getting cropped into a tiny scrollbar-bearing box with a blank second page. Verified properly the second time with an 8-section/48-step synthetic document in a deliberately small window: `printToPDF` produced a correctly-paginated 2-page PDF with every section present in order (`pdftotext`) and normal full-width layout (`pdftoppm`).
- **Export DOCX**: `lib/sop/markdownToDocx.ts` parses the rendered markdown with the same remark/unified stack the live preview uses and walks the AST into real `docx` structures — actual Word headings, numbered/bulleted lists (including one nesting level), GFM tables, bold/italic, code (shaded, monospace), blockquotes, and embedded images — not plain text in one style. Runs entirely client-side, same as Export .md, so nothing is sent anywhere for this. Document-wide defaults: Arial (`styles.default.document.run.font`) and 10pt space after every paragraph, with headings 1-6 additionally getting real space *before* them (18pt for the two levels this app's output actually uses — H1 title, H2 numbered sections — 14pt/12pt for 3-6 in case a hand-edited or imported document goes deeper) so sections read as visually distinct blocks in the exported file instead of running together, matching how they already look in the live preview/PDF. Code (`Courier New`) keeps its own explicit per-run font regardless of the document default, since code should stay monospace. Shipped once with a real regression, caught from a live exported file: the `docx` library's own heading defaults (`DefaultStylesFactory`) bake a `size`/`color` into each heading level (e.g. Heading1 is `{ color: "2E74B5", size: 32 }`) — that's the entire source of a heading's visual hierarchy, the "Heading 1"/"Heading 2" style IDs alone don't imply a size — and these per-level overrides get shallow-merged against that default at the `run` key, not deep-merged property-by-property. Supplying `run: { font: "Arial" }` alone replaced the *whole* default run object, silently dropping every heading's size and color and leaving every section heading the same size as body text (just bold). Fixed by re-stating each level's own default size/color explicitly alongside the Arial/spacing additions, rather than only adding what was new. Verified by unzipping a real generated `.docx` (it's a zip of OOXML) and checking `word/document.xml` for the expected heading/bold/table/numbering elements, `word/styles.xml` for Arial in `docDefaults` and both heading styles plus the expected `w:spacing` *and* `w:sz`/`w:color` values, confirming `word/media/` contains the embedded image byte-identical to the source, and confirming the document title lives in `docProps/core.xml` metadata rather than as a duplicate visible heading (an actual bug caught this way: an unconditional title paragraph collided with the markdown's own `# H1`). An unfilled `{{key}}` left in the document (see "No fabricated identifiers" above) gets the same amber shading/text color the live preview's `.unbound-placeholder` and its `@media print` override already use (`FEF3C7`/`92400E`) — `lib/sop/markdownToDocx.ts#splitPlaceholderRuns` splits any text, inline-code, or code-block run on `{{key}}` boundaries and applies the distinct styling only to the placeholder portion, so a still-unfilled field reads as visibly flagged in an exported .docx too, not just inside the app. Verified by generating a real file with both a plain-text and a backtick-wrapped unfilled variable and confirming both `<w:r>` runs carry the amber `w:shd`/`w:color`, with the backtick-wrapped one keeping its Courier New font alongside the new color.
- **Insert Image** (ActionBar) reads a local image file, base64-encodes it as a `data:` URI, and appends it as a standard `![]()` markdown image to the template — no upload, no external hosting, consistent with nothing generated being persisted anywhere but the document itself. Works automatically in Preview and PDF export (both already render whatever's in the DOM, including `data:` image src) and in DOCX export (`markdownToDocx.ts` decodes the data URI directly into the embedded media file). 5MB cap per image.
- **Regenerate** discards current field values/edits after a confirm prompt, then re-runs the same topic through the pipeline.
- **SLA mode regenerated as an SOP after the first generation.** Reported live right after SLA mode shipped: "the sla is making stuff that is formatted like an SOP." Root cause: `documentType` was only ever local state inside `TopicInput.tsx`, sent up to `SopWorkspace.tsx#generate()` as a one-shot function argument at submit time — but Regenerate (ActionBar) and the Guided flow's submit/skip both call `generate()` through their own code paths that never had that argument to pass, so they always fell back to the server's `"sop"` default regardless of which toggle position was showing. Same class of bug as the earlier Simple/Advanced work, just a different field falling through a different gap. Fixed by lifting `documentType` into `SopWorkspace.tsx` as real controlled state (the exact same pattern `category` already uses) instead of a one-shot argument — `TopicInput`'s toggle now calls `onDocumentTypeChange` directly instead of touching its own local state, and `generate()` reads `documentType` from that shared state on every call, so Regenerate/Guided/a plain Generate all agree with whatever the toggle currently shows, with no argument to forget to pass. Verified live with a mocked `/api/generate` route capturing the actual outgoing request bodies: selecting SLA, generating, then clicking Regenerate sent `documentType: "sla"` on both requests (previously the second would silently have been `"sop"` or missing entirely); the SOP path was re-verified the same way to confirm the fix didn't regress the default case.
- **SLA mode** — a second document type alongside SOPs, for drafting Service Level Agreements: coverage hours, severity tiers, response/resolution time targets, and an escalation path, rather than a procedure someone executes. Requested live: "I was tasked with creating an SLA for when we do work outside of business hours and for who. gives clear guidelines for time to do it and everything." A SOP/SLA segmented toggle sits next to Simple/Advanced in `TopicInput.tsx`; picking SLA swaps the topic placeholder, the Generate button's label, and — the part that actually matters — which system prompt gets used. `SLA_SYSTEM_PROMPT` in `lib/llm/prompt.ts` mirrors `SOP_SYSTEM_PROMPT`'s numbered seven-section structure and no-hedging/variable-parameterization discipline, but with SLA-appropriate sections instead (Purpose and Overview, Scope, Service Coverage and Hours, Severity Tiers, Response and Resolution Time Targets — a GFM table, one row per severity — Escalation Path, Roles and Responsibilities). Deliberately reuses the entire rest of the pipeline unchanged: the same output schema (`sopJsonSchema`/`sopZodSchema` — `template_markdown` is still just markdown, with nothing SOP-specific baked into the schema itself), the same variable form/Library/exports/Refine/category profiles, and the same Advanced-mode draft-terms textarea (reworded contextually — "paste your own draft terms" instead of "draft steps"). `app/api/generate/route.ts` accepts a new optional `documentType: "sop" | "sla"` field (defaults to `"sop"`, so every existing caller/test keeps working unchanged) and picks the matching system prompt; `buildUserPrompt` takes the same field to phrase its opening line correctly ("Generate a complete SLA for the following scenario" vs. "...SOP for the following task/technology/procedure"). Also tightened `REVIEW_IMPROVE_SYSTEM_PROMPT` with one line so it doesn't try to reshape a reviewed SLA into the SOP's seven-part shape — judged on its own terms instead, since that rule was previously written assuming everything reviewed was an SOP. Verified against a real local model (Ollama, `qwen2.5-coder:7b`) end-to-end: the UI toggle correctly shows the SLA-specific placeholder/button text and sends `documentType: "sla"` in the request; a real generation for "after-hours support for critical production outages" came back in the correct seven-section structure with a real GFM response/resolution-time table and a genuine escalation chain. Caught one real gap on the first attempt — several reusable values (an escalation contact, an organization name) came back as bracketed prose like `[VP of Operations Name]` instead of real `{{variable}}` fields, defeating the point of a fillable field — fixed with one explicit right/wrong example added to the variable rules ("a bracket isn't a form field"); a retest showed clear improvement (the escalation contact correctly became `{{escalation_contact}}`) though the organization/provider name still came back bracketed on that attempt — consistent with this same small quantized local model's uneven instruction-following seen elsewhere in this project, reported honestly rather than claimed as a clean fix, per this project's established practice of one targeted prompt pass rather than chasing 100% compliance from a resource-constrained test model.
- **Formal document-control structure for both SOP and SLA generation — a full structural overhaul, not a tweak.** Prompted by two real reference templates pasted live: a company-formal SOP template (document metadata header — Document ID, Version, Effective Date, Review Cycle, Owner, Approver — numbered sections with `5.1`/`5.2`/`5.3` subsections, a Revision History table) and, after a first SLA/policy template (severity tiers, a no-bypass intake rule, hardware-failure protocol) was directly compared against a second, structurally different one (uptime-percentage availability, a performance-metrics table, penalties/remedies, a Signatures & Approvals table) — the user picked the second explicitly ("Replace it — use this one instead"), so `SLA_SYSTEM_PROMPT` was rewritten a second time in the same pass. Both `SOP_SYSTEM_PROMPT` and `SLA_SYSTEM_PROMPT` in `lib/llm/prompt.ts` now reproduce their target structure *literally* in the prompt text (the header block, both horizontal rules, every heading, both tables) rather than describing it in prose, on the theory that a model follows a literal example more reliably than an equivalent description — SOP is `# Title` + a six-`{{variable}}` header block + `## 1. Purpose` through `## 7. Documentation & Ticket Closure` (with `### 5.1`/`5.2`/`5.3` inside "Step-by-Step Procedure") + `## 8. Revision History`; SLA is `# Service Level Agreement: {{service_name}}` + a similar header + `## 1. Agreement Overview & Scope` through `## 6. Penalties, Remedies & Reporting` + `## 7. Signatures & Approvals` (a real sign-off table with literal `____________________` signature-line cells) + `## 8. Revision History`. Per the user's explicit choice, every document-control field (Document ID, Version, Owner, Approver, signer names, etc.) is a real `{{variable}}` — set once and reused, consistent with how every other reusable value in this app works — not a blank bracket to hand-fill outside the Variables panel. `lib/llm/schema.ts`'s `template_markdown` description and both `REVIEW_IMPROVE_SYSTEM_PROMPT`/`REFINE_SYSTEM_PROMPT`'s stale "seven-section" references (hardcoded to the old SOP-only shape) were generalized to match, and `REVIEW_IMPROVE_SYSTEM_PROMPT` now brings a missing header block/Revision History to whatever structure the reviewed document type actually calls for rather than forcing every document toward SOP shape. Directly relearned this project's own earlier lesson (see the "seven-section structure" bullet above, from v0.2.23-era): reproducing an entire literal template plus a full prose rules section pushed `SOP_SYSTEM_PROMPT` to ~13,900 characters, and a first live verification against a real local model (Ollama, `qwen2.5-coder:7b`) came back with the section structure almost entirely ignored (an invented "4. Steps / 5. Rollback Steps / 6. Safety Checkpoints" shape) and the switch-serial-number example from "THE SINGLE MOST IMPORTANT RULE" itself violated — strong evidence of the same context-window degradation documented back then. Trimmed both prompts' explanatory prose (redundant restatement cut, examples condensed) back down to ~11,600/~8,300 characters without dropping any rule's substance, and a retest came back dramatically closer to compliant: correct section 1-6 structure for the SOP (missing only section 7), and for the SLA, all eight sections present in exactly the right order including both the Signatures and Revision History tables formatted exactly as specified — with a few header-block variables still landing as literal hardcoded text (e.g. "1.0", "Annual") rather than `{{}}` on that specific weak local model, reported honestly as a known limitation of the test model rather than a structural prompt failure, consistent with this project's established practice.
- **Dropped Document ID from the header, replaced Review Cycle with two explicit dates.** Follow-up feedback right after the document-control overhaul above: "remove the doc id from the sop and sla and with the review cycle might just add the review date and next review date." Both `SOP_SYSTEM_PROMPT` and `SLA_SYSTEM_PROMPT` in `lib/llm/prompt.ts` lost their `**Document ID:** {{document_id}}` header line entirely, and `**Review Cycle:** {{review_cycle}}` (a cadence like "Annual") became two separate fields — `**Review Date:** {{review_date}}` and `**Next Review Date:** {{next_review_date}}` — both genuinely unique per document with no common convention, so both default to `""` rather than a fabricated date, same treatment `{{effective_date}}` already got. Every rule bullet referencing the old six-variable header list, the default-value exceptions, and the variable-redundancy examples (which used `{{document_id}}` as the canonical "don't split one value across multiple variables" illustration) were updated in both prompts to match, along with `lib/llm/schema.ts`'s `template_markdown` description and `REVIEW_IMPROVE_SYSTEM_PROMPT`'s header-field listing — grepped the whole prompt file afterward to confirm no orphaned `document_id`/`review_cycle`/`policy_id` reference survived anywhere outside an unrelated pre-existing firewall-policy-ID example. This is a small, mechanical rename/removal rather than a new structural requirement, so it was verified by careful text review and `tsc` rather than a fresh multi-minute local-model round trip on top of the several already run for the structural overhaul above — two live verification attempts against the local test model that day did fail, but on unrelated generic JSON-schema-compliance issues (an empty `category`, a variable missing its `default` field) consistent with that specific quantized model's known flakiness under sustained load, not on anything tied to this specific edit.
- **Simple/Advanced generation settings.** The topic box used to always show every option (category, Attach Reference, Guided) at once; reported as noise for the common case of "just type a topic and go." `TopicInput.tsx` now has an inline Simple/Advanced segmented toggle above the form (Simple is the default, matching prior behavior exactly) — Simple hides the category field/profile picker, Attach Reference, and the Guided `?` button, leaving just topic + Import + New + Generate; Advanced reveals all of those plus a new "paste your own draft steps" textarea. When filled, that text is sent as a new `draftSteps` field (`app/api/generate/route.ts`, capped at 20,000 characters same pattern as the other optional blocks) and appended to the prompt as a distinct "Draft steps" block (`buildUserPrompt` in `lib/llm/prompt.ts`) that `SOP_SYSTEM_PROMPT` instructs the model to treat as the real raw material for the procedure — formalize it into the required structure/interaction-mode/bolding/variables, don't invent a different procedure or replace the user's specifics with generic ones, but fill genuine gaps (e.g. no rollback given) with standard best practice same as always. Verified live: toggling Advanced reveals/hides exactly the right controls with no change to Simple's existing behavior, and the draft-steps textarea's value round-trips into the outgoing `/api/generate` request body under `draftSteps`, redacted the same way the topic already is.
- **Arrow-key navigation between a field's occurrences.** Hover-to-locate (see below) only ever jumped to a field's *first* occurrence, which wasn't enough for a field reused many times across a long SOP — reported live as a request for something like Ctrl+F's next/previous match. Each Variables-panel row is now keyboard-focusable and shows an up/down chevron pair with an "n/m" counter whenever its `{{key}}` appears more than once in the current document (`occurrenceCounts` in `SopWorkspace.tsx`, computed by counting literal `{{key}}` matches against the raw template — cheap and doesn't depend on the live editor's decoration DOM being mounted). Clicking the chevrons, or pressing `ArrowUp`/`ArrowDown` once a row has focus, steps through that field's occurrences one at a time, wrapping at either end; `MarkdownPreview.tsx` scrolls to and highlights the specific indexed occurrence with a new, more prominent `.sop-var-nav-active` style (amber ring) distinct from the plain hover highlight, using the same `scrollBy`-not-`scrollIntoView` approach already established for hover-to-locate (see that bullet for why). This is deliberately separate state from hover (`activeNav` vs. `hoveredKey`) so mousing away from the field row doesn't lose your place mid-navigation. Verified live with Playwright, both by mouse (clicking the down-chevron three times on a field with 6 occurrences correctly advanced the "n/m" badge 1/6 → 2/6 → 3/6 and produced exactly one `.sop-var-nav-active` element in the live editor) and by keyboard (focusing a field row's label and pressing ArrowDown/ArrowDown/ArrowUp produced 1/3 → 2/3 → 3/3 → 2/3).
- **Split fields** — reported live as a real gap: when one `{{key}}` is reused at several points in a document, there was no way to make one specific occurrence independent of the others. Clicking a `{{key}}` chip in the Rendered tab now opens `SplitOccurrencePopover.tsx`, anchored to that exact chip, with two actions scoped to **that one occurrence only**: reassign it to point at a different already-declared field, or split it off into a brand-new field (key auto-suggested as `${originalKey}_2`, editable) seeded with the original field's current value, type, and description, so the document renders identically right after the split — only future edits to either field diverge. Chips were already atomic (not editable in place, per "Live editable Rendered view" above) and had no other click behavior, so repurposing a click for this doesn't take anything away. The click handler reports the occurrence's exact document offsets (`liveMarkdownExtensions.ts`'s `VariableChipWidget`, extended with an `onOccurrenceClick` callback alongside the existing hover one), and the popover's two actions are plain string splices on `template` at those offsets (`SopWorkspace.tsx#handleReassignOccurrence`/`handleCreateFieldFromOccurrence`) — no new document model, consistent with the rest of this app's markdown-string-is-the-source-of-truth design. The popover closes itself on any template change between opening and confirming (`MarkdownPreview.tsx`, a `useEffect` keyed on `template`), so a stale offset can't be applied even if something else edits the document in between. Caught and fixed a real CodeMirror bug while wiring the click handler up: `VariableChipWidget`'s `eq()` only compared the chip's visible fields (key/display/emptiness/position), not its callback references — since the editor's first paint happens with `liveVariablesField`'s empty default (no callbacks yet) before the resync effect dispatches the real ones a moment later, `eq()` saw that transition as "no change" and told CodeMirror to keep reusing the very first DOM node, which had no click listener attached at all; clicking a chip did nothing, permanently, for the editor's whole lifetime. Fixed by including the callback references in `eq()` too — since they're React state setters with stable identity across re-renders, this only forces one real DOM rebuild at the mount→resync transition, not on every later keystroke. Verified live with Playwright: reassigning one of six `{{app_name}}` occurrences to `{{server_host}}` left exactly five `app_name` chips and one more `server_host` chip in the document with no other field affected; splitting a different occurrence into a new field produced a correctly-seeded new `App Name (2)` entry in the Variables panel and swapped just that one chip to the new key; and clicking outside the popover closes it without applying anything.
- **Remove Field could silently wipe every other field, not just the one removed — and the same overshoot could happen through any free-text Refine instruction, not just the Remove-field button.** First reported as "I deleted one variable field and it deleted all of them," fixed for that specific button (see below), then reported again through a completely different path on an SLA document: "I did the ai edit for an sla and it literally got rid of my variables." Remove Field works by sending the whole document through Refine with a synthesized "remove only this one variable" instruction; `validateAndReconcile` only checks the model's response for *internal* consistency (every `{{key}}` in the text has a `variables[]` entry and vice versa) — it has no concept of what existed *before* the call, so if the model overshot and regenerated large parts of the document, quietly dropping other `{{key}}` occurrences along the way, that response looked perfectly valid and got applied as-is. Reproduced live against a real local model (Ollama, `qwen2.5-coder:7b`) with a multi-variable document and a single-field removal request — that attempt actually held the line correctly (only the targeted variable disappeared, the other four survived byte-identical), consistent with this being an occasional overshoot rather than a guaranteed failure, which is exactly why it's dangerous: it can look fine in testing and still fail on a real document. Fixed with a code-level guard, not just another prompt tweak, since prompt compliance alone already wasn't reliable enough for a destructive action: `handleRefine` in `SopWorkspace.tsx` takes an optional `requiredKeys` list — every key in it must still be present in the response, or the whole response is rejected outright (nothing applied), with the error banner naming exactly which field(s) would have been unexpectedly lost. `handleRemoveField` passes every *other* currently-declared key explicitly, since it knows precisely what should survive. The second report exposed the real gap: a free-text instruction typed into `RefinePanel.tsx` (e.g. "make this sound more formal," never naming any specific field) went through `handleRefine` with no `requiredKeys` at all — zero protection. Rather than trying to special-case every UI entry point, `handleRefine` now computes a default `requiredKeys` itself whenever the caller doesn't pass one: any currently-declared field whose key or label doesn't appear anywhere in the instruction text is assumed untouched by it and required to survive. An instruction that does name a field (e.g. "remove the server_host field, it's not needed") correctly excludes that field from the required set, so a legitimate targeted removal still goes through — only an *unnamed* field disappearing gets rejected. This heuristic isn't perfect (a fully generic instruction that names nothing marks every field required, which could reject a legitimately sweeping edit) — but that's the deliberately safer failure mode: worst case you retype the instruction more specifically, instead of silently losing fields with no signal at all. `REFINE_SYSTEM_PROMPT`'s field-removal rule was also strengthened with an explicit line that removing one variable is never grounds to touch any other one, as a first line of defense — but the guard is what actually prevents data loss when that line isn't enough. Verified live with Playwright for both fixes: mocking `/api/refine` to drop every variable in response to the dedicated Remove-field button showed both fields surviving untouched with the guard's error shown instead; separately, a vague free-text instruction that dropped an unmentioned field was rejected the same way, while an instruction explicitly naming the field to remove was correctly allowed through.
- **Refine's overshoot got caught, but a rejection wall isn't a fix — the AI still needed to stop overshooting in the first place.** Follow-up feedback right after the guard above shipped: "how about we make it not drop it?? I told you that it should edit the existing. not recreate it" — a fair point, since a Refine session that just rejects every edit attempt on a large document isn't actually usable. The `requiredKeys` list `handleRefine` already computes (see above) is now ALSO sent proactively to the AI itself, not just checked against its response after the fact — `buildRefinePrompt` in `lib/llm/prompt.ts` gains an optional `protectedKeys` parameter, threaded through `app/api/refine/route.ts` (`protectedKeys` request field, capped at 200) from `SopWorkspace.tsx`'s `handleRefine`. `REFINE_SYSTEM_PROMPT` also gained a "THE SINGLE MOST IMPORTANT RULE" banner (matching the pattern `SOP_SYSTEM_PROMPT` already uses for its own top rules) reframing the whole task as editing-not-rewriting: treat everything the instruction doesn't touch as text to copy forward byte-for-byte, not to regenerate from scratch, with an explicit note that this matters most on exactly the kind of long, heavily-parameterized document (many severity tiers, many contacts) where regenerating does the most damage. Verified against a real local model (Ollama, `qwen2.5-coder:7b`) on a synthetic 10-variable SLA closely matching the reported scenario (multiple severity tiers, coverage hours, escalation contact, all still unfilled placeholders) with a narrow instruction ("change the Tier 1 response target to 15 minutes"): the response correctly changed only that one value and preserved all nine other variables and the entire rest of the document byte-for-byte. Caught and fixed a real regression along the way from the `protectedKeys` prompt block's first placement: sitting in a `---`-delimited block directly between the document and the instruction, the small local model read it as part of the document itself and echoed the entire reminder text verbatim into its response, plus fabricated a spurious extra variable. Fixed by moving the block to after the instruction, phrased as a parenthetical "reminder, not part of the document" with an explicit "don't include this in your output" line — retested and the echo/fabrication were gone, with the correct single-field edit still applying cleanly.
- **Refine with AI stopped accepting input after the first message.** Reported live as "the refine with ai now does not let me type." Root cause, confirmed with a scripted repro: the instruction `<input>` is `disabled` while a request is in flight, and browsers auto-blur an input the instant it becomes `disabled` — after the first Apply completed and the field re-enabled, focus had silently landed on `<body>` and nothing ever moved it back, so continuing to type the next instruction (without first re-clicking the field, the natural expectation in a chat-like panel) did nothing at all. Fixed in `RefinePanel.tsx` with a `useEffect` that watches `refining`'s true→false transition and calls `.focus()` on the input again once the request settles (success or failure). Verified by reproducing the exact broken sequence first (typed and submitted one instruction, polled `document.activeElement` afterward — it was `<body>`, and blind-typing a second instruction produced an empty value) and then confirming the fix (same sequence now leaves the input focused throughout, and the second instruction's text lands correctly with no re-click).

<!-- last verified: install/update/uninstall lifecycle tested end-to-end -->
